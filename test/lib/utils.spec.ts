// import { getJsonProcessor } from '../../src'
// import JsonDataType from '../../src/lib/JsonDataType'

describe('utils', () => {
  describe('getJsonProcessor', () => {
    it('should return processor', () => {
      // getJsonProcessor({ json: { type: 'func' } }, JsonDataType)
      expect(1).toBe(1)
    })
  })
})
